# Weft releases

Published build artifacts for **Weft** (signed + notarized macOS DMG/ZIP + latest-mac.yml).
This is the auto-update feed read by electron-updater. App source is in a separate private repo.
